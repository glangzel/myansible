---
# roles/common/tasks/main.yml
- name: Install common packages
  apt:
    name:
      - git
      - tmux
      - fzf
      - ripgrep
      - neovim
      - curl
      - wget
      - build-essential
      - clang
      - stow
      - htop
    state: present
    update_cache: yes
