---
# roles/common/tasks/main.yml
- name: Install common packages
  apt:
    name:
      - git
      - tmux
      - neovim
      - curl
      - wget
      - build-essential
      - clang
      - stow
      - htop
      - evince
      - mupdf
      - thunar
    state: present
    update_cache: yes
